# Quarkus configuration
# HTTP port (default is 8080 — shown here so it is easy to change)
quarkus.http.port=8080

# Friendly banner in the dev console
quarkus.application.name=hello-world-static-quarkus
